import React from 'react';
import OnOffButton from '../containers/OnOffButton';
import SignupForm from '../forms/SignupForm';

const OnkibotApp = ({ children }) => (
    <div>
        {children}
    </div>
);

export default OnkibotApp;